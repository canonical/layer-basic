# common bootstrap
pip<23
setuptools<65
setuptools-scm<8
wheel<0.38

# common build dependencies for install-from-source-on-target approach
#
# these are unfortunately required because `pip download` does not support
# automatic discovery of build dependencies (ref pypa/pip#7863).
#
# building the charms with binary wheels (ref juju/charm-tools#620) would
# relieve us from having to track theese manually.
cython<0.30
flit_core<4

# common dependencies for all reactive charms
charmhelpers>=0.4.0,<2.0.0
charms.reactive>=0.1.0,<2.0.0

